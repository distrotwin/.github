# 门面仓库维护指引

这个仓库只有一件事：`profile/README.md` 渲染成 org 主页门面。它是对外页面，语气与信息都按「外人第一次看到」来写。

## 硬性约定

- commit **不允许带 co-author**
- 中文；Markdown **自然段内不换行**，一段写成一行长句
- 不谈许可与法务
- 内部工作口径不上门面：像「判据挂在结果上」「量的是尺子不是被试」「xfail 基线」这类内部讨论用语，READ ME 上要换成外人能懂的平实说法；内部机制细节一律链接到 buildkit 的 docs 而不是展开

## 名录的编辑规则

- **一行一个 OS**，不许合并
- 现状列与如何使用列**必须以图例内的 emoji 开头**：现状 🟢 维护中 / 🟡 状态存疑 / ⚫ 停滞或已并入；如何使用 ✅ 本 org / 📦 官方已有 / ⏳ 待定 / 🚫 不做
- 「如何使用」三分判据：本 org 支持的指过来；官方已有可用镜像的指官方；两者皆无的如实写原因与是否列入计划，不做要给原因（无介质 / 无源 / 定位错位 / 已被覆盖）
- **官方镜像引用写上去之前必须匿名 `docker manifest inspect` 实测可拉**，拉不到的 tag 不许出现（OpenCloudOS 的 `latest` 就拉不到，实测出 `:9.0` 才写的）
- 版本与日期来自实测或厂商原文，写「截至」年月；更新名录时同步更新该行全部四列，不许只改一列

## 与其它仓库的关系

各 OS 的基线数字、tag 规则、已知怪癖以对应仓库 README 为准，门面只做索引不做副本——两处写同一个数字，日后必有一处过期。
